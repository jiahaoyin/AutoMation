# Apple Profile Capture And Terminal Output Plan

1. Add a dedicated structural personal-information readiness/session check that
   waits for the exact route and stable name/birthday cards before classifying
   the profile page.
2. Move the requested screenshot to the confirmed information-page boundary,
   rename it to `02-account-information.png`, and remove the premature
   account-home screenshot from the public result contract.
3. Preserve birthday-first and modal-name extraction, then persist validated
   values through the existing private `.env` writer and print them only in an
   interactive direct terminal.
4. Split complete audit forwarding from terminal presentation: keep every
   sanitized backend event in `flow-audit.jsonl`, while mapping default terminal
   output to concise progress lines and retaining an explicit redacted debug mode.
5. Update Python/Node regression tests, documentation, screenshot metadata, and
   release contracts.
6. Run focused and full Windows-safe checks, complete two independent read-only
   reviews with a repair loop, then commit and push the reviewed branch.
